interface:
  display_name: "Rust Strict"
  short_description: "Strict Rust workflow, verification, API, unsafe, errors, and drift control"
  default_prompt: "Use $rust-strict to build, refactor, debug, review, or document this Rust project with strict Cargo, lint, API, unsafe, error-handling, drift-control, and verification discipline."
